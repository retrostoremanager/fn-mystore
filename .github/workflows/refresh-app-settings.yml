name: Refresh App Settings

on:
  workflow_dispatch:

jobs:
  refresh-settings:
    runs-on: ubuntu-latest

    steps:
    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Apply app settings
      env:
        JWT_SECRET_KEY: ${{ secrets.JWT_SECRET_KEY }}
        POSTGRES_ADMIN_PASSWORD: ${{ secrets.POSTGRES_ADMIN_PASSWORD }}
        STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
        STRIPE_PUBLISHABLE_KEY: ${{ secrets.STRIPE_PUBLISHABLE_KEY }}
        STRIPE_WEBHOOK_SECRET: ${{ secrets.STRIPE_WEBHOOK_SECRET }}
        STRIPE_PRICE_ID_MONTHLY: ${{ secrets.STRIPE_PRICE_ID_MONTHLY }}
        STRIPE_PRICE_ID_YEARLY: ${{ secrets.STRIPE_PRICE_ID_YEARLY }}
        CORS_ALLOWED_ORIGINS: ${{ secrets.CORS_ALLOWED_ORIGINS }}
        ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      run: |
        az functionapp config appsettings set \
          --resource-group mystore-rg-dev \
          --name mystore-func-dev \
          --settings \
          "ConnectionStrings__DefaultConnection=Host=mystore-postgres-dev.postgres.database.azure.com;Port=5432;Database=mystore-db;Username=sqladmin;Password=${POSTGRES_ADMIN_PASSWORD};SSL Mode=Require" \
          "JwtAuthentication__SecretKey=${JWT_SECRET_KEY}" \
          "Stripe__SecretKey=${STRIPE_SECRET_KEY}" \
          "Stripe__PublishableKey=${STRIPE_PUBLISHABLE_KEY}" \
          "Stripe__WebhookSecret=${STRIPE_WEBHOOK_SECRET}" \
          "Stripe__PriceIdMonthly=${STRIPE_PRICE_ID_MONTHLY}" \
          "Stripe__PriceIdYearly=${STRIPE_PRICE_ID_YEARLY}" \
          "Cors__AllowedOrigins=${CORS_ALLOWED_ORIGINS}" \
          "Anthropic__ApiKey=${ANTHROPIC_API_KEY}"

    - name: Restart Function App
      run: |
        az functionapp restart \
          --resource-group mystore-rg-dev \
          --name mystore-func-dev
        echo "App settings refreshed and Function App restarted."
